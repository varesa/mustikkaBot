Say (module)
============

"Say" is a module that will repeat the text that is passed after the command itself, if the user has access to the command.
It is mostly for fun, but can also be used to issue commands.